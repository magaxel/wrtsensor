{
  "config": {
    "step": {
      "user": {
        "title": "wrtsensor — Connect to OpenWrt",
        "data": {
          "gateway_host": "Gateway IP address (optional)",
          "ssh_key_path": "SSH private key path",
          "ssh_port": "SSH port",
          "ap_hosts": "Access point IPs"
        },
        "data_description": {
          "gateway_host": "IP address of the OpenWrt router that acts as your gateway. Leave empty if you only have OpenWrt access points behind a non-OpenWrt router. A single combined OpenWrt box (routing + Wi-Fi) counts as a gateway — enter its IP here.",
          "ssh_key_path": "Absolute path to the SSH private key on the HA host used to authenticate as root. Example: /config/ssh/id_ed25519",
          "ssh_port": "SSH port on all OpenWrt hosts (gateway and APs). Default: 22.",
          "ap_hosts": "Comma-separated IPs of OpenWrt access points. At least one host (gateway or AP) is required. Example: 192.168.1.22,192.168.1.23"
        }
      },
      "provision_key": {
        "title": "Add SSH key to OpenWrt devices",
        "description": "Key authentication failed. Enter credentials to automatically add the public key to each OpenWrt device. The password is not stored.",
        "data": {
          "ssh_user": "Username",
          "ssh_password": "Password"
        },
        "data_description": {
          "ssh_user": "SSH username on the OpenWrt device. Default: root.",
          "ssh_password": "SSH password. Used once to provision the key, never stored."
        }
      },
      "reconfigure": {
        "title": "Reconfigure wrtsensor",
        "description": "Change gateway, access points, SSH key path or SSH port. Every host is re-tested; removing a host prunes its per-host sensors on the next reload.",
        "data": {
          "gateway_host": "Gateway IP address (optional)",
          "ssh_key_path": "SSH private key path",
          "ssh_port": "SSH port",
          "ap_hosts": "Access point IPs"
        },
        "data_description": {
          "gateway_host": "Leave empty for APs-only mode. Any OpenWrt box that routes counts as a gateway.",
          "ssh_key_path": "Absolute path to the SSH private key on the HA host. Example: /config/ssh/id_ed25519",
          "ssh_port": "SSH port on all OpenWrt hosts. Default: 22.",
          "ap_hosts": "Comma-separated IPs of OpenWrt access points. At least one host (gateway or AP) is required."
        }
      }
    },
    "error": {
      "ssh_key_not_found": "SSH key file not found at the specified path",
      "auth_failed": "SSH authentication failed — check key and root access",
      "cannot_connect": "Cannot connect — check IP and network",
      "no_response": "Connected but host returned no hostname",
      "pub_key_unreadable": "Cannot read public key from the private key file",
      "provision_auth_failed": "Password authentication failed — check username and password",
      "provision_cannot_connect": "Cannot connect to device for provisioning — check IP and network",
      "setup_failed": "Setup failed: {failures}",
      "provision_failed": "Could not add the SSH key on: {failures}",
      "auth_failed_after_provision": "Key was provisioned but some hosts still reject it: {failures}",
      "at_least_one_host": "Enter at least a gateway or one access point."
    },
    "abort": {
      "already_configured": "This host is already configured",
      "reconfigure_successful": "Configuration updated"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "wrtsensor options",
        "data": {
          "ssh_key_path": "SSH private key path",
          "ssh_port": "SSH port",
          "ap_hosts": "Access point IPs",
          "scan_interval": "Scan interval (seconds)",
          "lan_iface": "LAN bridge interface",
          "wan_iface": "WAN interface",
          "disconnect_threshold_s": "Disconnect threshold (seconds)",
          "presence_macs": "Presence MACs",
          "enable_dns_stats": "Collect DNS stats",
          "enable_wireguard": "Show WireGuard connections",
          "wg_stale_threshold_s": "WireGuard idle timeout (seconds)"
        },
        "data_description": {
          "ssh_key_path": "Absolute path to the SSH private key on the HA host. Example: /config/ssh/id_ed25519",
          "ssh_port": "SSH port on all OpenWrt hosts (gateway and APs). Default: 22.",
          "ap_hosts": "Comma-separated IPs of OpenWrt access points to collect Wi-Fi associations and host stats from. Example: 192.168.1.22,192.168.1.23",
          "scan_interval": "How often to poll the gateway and APs. Minimum 30 s, maximum 3600 s. Default: 60 s.",
          "lan_iface": "Bridge interface used for the LAN on OpenWrt. Default: br-lan. Change only if your router uses a different name.",
          "wan_iface": "WAN-facing interface on the gateway used to read RX/TX byte counters. Default: eth0.",
          "disconnect_threshold_s": "How long a device must be absent before a disconnect event is recorded. Default: 120 s (2 missed scans at 60 s interval).",
          "presence_macs": "Comma-separated MAC addresses to track as presence binary sensors. Each MAC becomes a binary_sensor.wrtsensor_presence_* entity. Example: aa:bb:cc:dd:ee:ff,11:22:33:44:55:66",
          "enable_dns_stats": "Signal dnsmasq each scan to collect cache hit-rate, latency, and per-upstream history for the DNS stats card. Disable to skip the SIGUSR1 + logread on every scan and the DNS history file I/O. Requires a configured gateway. Default: on.",
          "enable_wireguard": "Auto-detect WireGuard on the gateway and APs each scan, then expose per-peer device_trackers and a card with handshake age, transfer totals, and live rate. Hosts without `wg` installed are skipped silently. Private and preshared keys are never read into HA — only public keys, descriptions, endpoints, and counters. Default: off.",
          "wg_stale_threshold_s": "Peers whose last handshake is older than this go to not_home and grey out in the card. WireGuard handshakes every ~2 minutes when active. Default: 180."
        }
      }
    },
    "error": {
      "ssh_key_not_found": "SSH key file not found at the specified path",
      "auth_failed": "SSH authentication failed",
      "cannot_connect": "Cannot connect",
      "no_response": "Host returned no hostname",
      "setup_failed": "Setup failed: {failures}",
      "at_least_one_host": "Enter at least a gateway or one access point."
    }
  }
}
